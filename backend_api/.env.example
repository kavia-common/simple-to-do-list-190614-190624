# Example environment variables for backend_api container
# The orchestrator should populate a real .env with valid credentials for the database container.

# PostgreSQL URL in SQLAlchemy format using psycopg3 driver
# e.g., postgresql+psycopg://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgresql+psycopg://postgres:password@database:5432/todos
